#include "main.h"

/**
 * more_numbers - print 0 - 14 times and you
 * you can only use _putchar three times
 *
 * Return: always 0
*/

void more_numbers(void)
{
	int i, j, k;

	for (j = 0; j <= 10; j++)
	{
		for (i = 0; i <= 14; i++)
		{
			i = k
			if (i > 9)
			{
				_putchar (1 + 48);
				k = i % 10;
			}
		_putchar (k + 48);
		}
		_putchar ('\n');
}
